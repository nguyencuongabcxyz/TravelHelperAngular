export class Trip {
    publicTripId?;
    arrivalDate?: Date;
    destination?: string;
    departureDate?: Date;
    travelerNumber?: number;
    description?: string;
    createDate?;
    isDeleted?;
    applicationUserId?;
}