export class Trip {
    publicTripId?;
    arrivalDate?;
    destination?: string;
    departureDate?;
    travelerNumber?: number;
    description?: string;
    createDate?;
    isDeleted?;
    applicationUserId?;
}
